# Codex

Start the verified HTTP transport:

```bash
npm run build
npm run start:http
```

Point an MCP-capable Codex environment at `http://127.0.0.1:8765/mcp`.
Suggested task: “Run the release-gate workflow and explain each vulnerability
and recommended fixed version using only returned evidence.”
