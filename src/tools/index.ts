/**
 * Tools barrel. Real tool definitions are added in AC-2/AC-3. This
 * stub exists so the registry has a stable import target during
 * scaffold.
 */
import type { ToolDefinition } from './types.js';

export const toolStubs: ReadonlyArray<ToolDefinition> = [];
